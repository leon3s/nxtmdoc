import React from 'react';

import * as Style from './style';

export type PropsSeparator = {
};

const Separator = ({}: PropsSeparator) => (
  <Style.ContainerSeparator />
);

export default Separator;
